server:
  port: 9000

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/VighneshG/config-server
          clone-on-start: true